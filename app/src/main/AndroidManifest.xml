<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maps.optadmin.maps" >

    <uses-permission android:name="com.example.googlemaps.permission.MAPS_RECEIVE" />
    <permission
        android:name="com.example.googlemaps.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBJ5GC0U5c9lFMUooS-uhPxuet5YC4f62M"/>
        // Status = PLACES_API_LIMIT_EXCEEDS Stauts CODE = 9001
        /*This error can be caused by not enabling the 'Places API for Android' in your google developer console (This is different from the regular 'Places API').

        It can also be caused if you set your API key using the old "com.google.android.maps.v2.API_KEY" identifier instead of "com.google.android.geo.API_KEY", when adding the google meta-data to your application manifest.*/

// AIzaSyBJ5GC0U5c9lFMUooS-uhPxuet5YC4f62M  // jabduloptmx@gmail.com
        // AIzaSyDPFTcN5ekX99mHrczkgLnLv9WMzp4pWYg //ma.juber@gmail.com
    </application>

</manifest>
